[package]
name = "discount_calculator_plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-sdk = { path = "../../shared/plugin-sdk" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

