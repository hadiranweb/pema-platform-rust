[workspace]
members = [
    "wasm-frontend",
    "backend-server",
    "backends/installer",
    "shared/config",
    "wasm-auth-backend",
    "wasm-general-backend",
]

[patch.crates-io]
# Optional: If you have local patches for crates

