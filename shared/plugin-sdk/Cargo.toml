[package]
name = "pema_plugin_sdk"
version = "0.1.0"
edition = "2021"


[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1.18.1", features = ["serde", "v4"] }

[lib]
crate-type = ["cdylib", "rlib"]


uuid = { version = "1.0", features = ["serde", "v4"] }

